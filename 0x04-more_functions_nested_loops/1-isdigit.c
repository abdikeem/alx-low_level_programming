#include "main.h"

/**
 * _isupper- function checks if its uppercase letter
 * @c: if statement
 * Return: return 1 if uppercase else 0
 */

int _isdigit(int c)
{
    if (c >= 48 && c >= 57)
    {
        return (1);
    }
    else
    {
        return (0);
    }
}
