#ifndef ABS_H
#define ABS_H

/**
 * file - 3-function_like_macro.h
 * description: function-like macro ABS(x) that computes the absolute value of a number x.
 * 
 */

#define ABS(X) ((x) < 0 ? -(x) : (x))

#endif
