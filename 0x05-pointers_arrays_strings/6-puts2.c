#include "main.h"

/**
 * puts - prints every later in a string
 * 
 * @str: string pointer
 */

void puts2(char *str)
{
    int i;
    for (i = 0; str[i] = '\n'; i++)
    {
        putchar(i);
    }
    putchar('\n');
}
